# 暖纸“编辑手记”整站设计 QA

## 验收范围

- 参考图：Product Design 第二版“编辑手记”
- 桌面视口：1440 × 1024
- 移动视口：390 × 844
- 页面：首页、博客列表与详情、笔记、灵感、AI 导航、公众号、登录、后台概览、内容列表、新建内容
- 核心流程：登录、AI 搜索、Markdown 编辑/分屏/预览、发布保存、公开列表与详情阅读、本地 Markdown 导入入口

## 参考图对照

| 对照项 | 参考图特征 | 当前实现 | 结果 |
| --- | --- | --- | --- |
| 页面骨架 | 左侧导航、中部书写主栏、右侧阅读索引 | 1440 视口实测为 `236px / 930px / 264px` 三栏 | 通过 |
| 材质与色彩 | 奶油暖纸、墨褐正文、陶土红与苔藓绿 | 全站统一纸纹 WebP、墨色文字和双色强调 | 通过 |
| 字体层级 | 编辑部衬线标题、克制的小型英文眉题 | 首页宣言、列表标题、详情正文和后台标题共用同一层级系统 | 通过 |
| 分隔与表面 | 细档案线、轻圆角、极弱阴影 | 公共页与后台均使用细线和纸面层次，无玻璃拟态与发光阴影 | 通过 |
| 装饰资源 | 植物线稿与纸面图像 | 使用真实纸纹与水墨植物 WebP；不依赖可见内联 SVG | 通过 |
| 响应式 | 参考图为桌面编辑部布局 | 移动端改为品牌栏与导航抽屉，内容转单栏 | 通过 |

当前实现有一项有意差异：首页采用更强的“记录思考，让知识缓慢生长”宣言作为首屏焦点，并以真实内容数据驱动连续文章列表，不使用参考图中的示例照片卡片。该差异保持了选定方向的编辑部气质，同时更符合个人知识站的实际数据边界。

## 浏览器验收证据

### 桌面端

- 首页三栏比例为 `236px / 930px / 264px`，页面横向溢出为 `0`。
- 博客列表正确显示已发布文章；详情页只保留一个一级标题。
- 详情 Markdown 实测包含任务列表、表格、引用和代码块。
- AI 搜索框输入 `Cursor` 后只展示匹配内容。
- 笔记、灵感、公众号、登录、后台概览、内容列表和新建内容页面均正常渲染且无横向溢出。
- 后台“内容”与“标签”不会同时高亮。
- 新建内容默认分屏；预览结构正确；文章可发布、保存并回到公开列表与详情。

### 移动端

- 首页隐藏桌面双侧栏，显示 22px 菜单图标；导航抽屉可打开和关闭。
- 首页、AI 导航、登录、后台概览、内容列表和后台编辑页横向溢出均为 `0`。
- 后台编辑页正文区位于发布设置之前，保存和导入入口可达。
- Markdown 默认进入“编辑”模式：编辑列 `314px` 全宽，预览列隐藏。
- 切换到“分屏”时左右列各 `157px`；切回“编辑”恢复 `314px`；“预览”模式可正常进入并返回，过程中无 500。
- 浏览器错误与警告日志为空。

### 本地 Markdown 导入

- 浏览器已验证 `.md` / `.markdown` 选择入口、文件类型提示和导入按钮可见。
- 当前浏览器控制接口不提供本地文件注入能力，因此实际 `File.text()` 读取、扩展名校验、覆盖确认和表单写入由单元测试与组件集成测试覆盖；导入不会自动保存或上传文件。

## 问题分级与处理

| 等级 | 问题 | 处理结果 |
| --- | --- | --- |
| P0 | Nuxt 自动组件名误写为 `ContentContentList`，公开列表运行时无法解析 | 已改为 `ContentList` 并补回归测试 |
| P1 | 文章页头标题与 Markdown 首个 H1 重复 | 仅在标题完全相同时移除正文首个 H1，并覆盖四类详情页 |
| P1 | Carbon 图标资源未安装，移动菜单与操作按钮为空框 | 补充 `@iconify-json/carbon`，实测图标尺寸与遮罩正常 |
| P1 | 移动端编辑模式仍保留空白预览列，并出现组件重挂载竞态 500 | 保持第三方实例稳定，用模式样式控制分栏；实测全宽、零溢出、三种模式正常 |
| P2 | 小屏先显示发布设置，正文编辑区排在后面 | 620px 以下恢复正文优先顺序 |
| P2 | 后台“内容”和“标签”入口同时高亮 | 查询参数入口改为完整地址匹配 |
| P3 | 首页宣言比参考图更强，未使用示例文章照片 | 有意保留，符合真实内容驱动与个人品牌定位 |

## 自动化门禁

最终验证时间：2026-07-13 08:55–08:57 CST。

| 门禁 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过，退出码 0 |
| `pnpm lint` | 通过，退出码 0 |
| `pnpm test:run` | 32 个测试文件、107/107 测试通过 |
| `pnpm build` | 通过，生成 Nitro Node Server 产物；仅有 md-editor 第三方分块体积提示 |
| `git diff --check` | 通过，退出码 0 |
| 旧视觉运行时代码扫描 | `AppAuroraBackground`、玻璃/极光类名零命中 |

final result: passed

## 2026-09-19 AI 导航与博客合并复验

- AI 导航改为 AIHOT 精选资讯页，桌面端保留左中右三栏暖纸结构，采用“分类筛选、当前热点、资讯时间流”布局。
- AIHOT 公开 API 实测返回 50 条精选资讯和 5 条热点；服务端缓存 60 秒，并过滤非 HTTPS 外链。
- 桌面端实测分类“模型”由 50 条筛选为 5 条，搜索 `OpenAI` 得到 14 条；来源署名和外链属性正确。
- 390 × 844 移动端实测无页面级横向溢出，分类栏仅自身横向滚动；导航抽屉中 AI 导航位于第一位。
- `/` 显示博客列表，侧栏、页脚和右侧阅读索引均只保留一个博客入口；`/blog` 实测返回 301 并落到 `/`。
- `pnpm test:run`：34 个测试文件、114/114 通过。
- `pnpm build`：退出码 0；仅保留既有第三方编辑器分块体积提示。

final result: passed

## 2026-09-19 AIHOT 全量接入与本站详情页复验

验收方式：生产构建产物 `node .output/server/index.mjs`（127.0.0.1:3100）+ 系统 Chrome 无头浏览器实测。

### 9 个端点接入

`/api/v1/items`、`/codex-resets`、`/hot-topics`、`/stories/{publicId}`、`/dailies`、`/dailies/latest`、`/dailies/{date}`、`/selected/snapshot`、`/selected/changes` 全部打通，服务端统一收敛在 `server/api/ai-nav.get.ts`。

实测中发现并与真实响应对齐的契约细节：

- `/dailies/latest` 的 `report.lead` 实测为 `null`，`flashes` 存在但常为空数组；日报标题退回稳定的 `YYYY-MM-DD AI 日报`。
- `/selected/snapshot` 的 `count` 是当前页条数而非总量（`limit=1` 时为 1），因此不再对外暴露为“同步总量”，页脚改为展示同步水位与本轮变化数。
- `/hot-topics` 的 `links.story` 域名是 `aihot.virxact.com`，story id 可直接用于 `aihot.news/api/v1/stories/{id}`。
- `/items` 没有单条查询端点；热点条目可能已滑出最近 100 条精选，改用 `mode=all&q=<标题>` 回查补齐摘要、评分与推荐理由。

### 页面实测

| 路由 | 视口 | 结果 |
| --- | --- | --- |
| `/ai` | 390 × 844 | HTTP 200，列表页内容正确，横向溢出 0 |
| `/ai/{id}` | 390 × 844 / 1440 × 900 | HTTP 200，含 AI 导读、AI 评分、推荐理由、查看原始信源 |
| `/ai/{hotId}` | 390 × 844 | HTTP 200，含事件追踪、事件时间线（实测 4 篇报道）、一手信源标记、进行中状态 |
| `/ai/daily` | 390 × 844 | HTTP 200，日报归档页内容正确 |
| `/ai/daily/2026-09-19` | 1440 × 900 | HTTP 200，日报详情含分区与快讯 |
| `/ai/items/{id}` | — | HTTP 301 跳转到 `/ai/{id}` |
| `/ai/__nope__` | — | HTTP 404 |
| `/api/ai-dailies/2026-99-99` | — | HTTP 400 |

5 条热点跳转详情页全部带摘要与评分，其中 2 条标签类条目按设计不显示推荐理由；浏览器控制台错误与页面异常均为 0。

### 修复的 P0 问题

| 等级 | 问题 | 处理结果 |
| --- | --- | --- |
| P0 | `pages/ai.vue` 作为 `pages/ai/**` 的父路由且缺少 `<NuxtPage />`，导致 `/ai/daily`、`/ai/daily/:date`、`/ai/:id` 全部渲染成列表页 | 下沉为 `pages/ai/index.vue`，并补“父子路由不得并存”回归测试 |
| P1 | 热点条目滑出精选窗口后详情页只剩标题 | 增加 `/items?mode=all&q=<标题>` 回查，失败时降级为热点快照而非报错 |
| P2 | 页脚“N 条同步记录”取自 snapshot 单页条数，恒为 1 | 改为展示精选同步水位与本轮变化数 |
| P2 | 分类栏存在 AIHOT 不返回的 `opinion` 死标签 | 移除该筛选项 |

### 自动化门禁

| 门禁 | 结果 |
| --- | --- |
| `pnpm lint` | 通过，退出码 0 |
| `pnpm typecheck` | 通过，退出码 0 |
| `pnpm test:run` | 34 个测试文件、121/121 测试通过 |
| `pnpm build` | 通过，Nitro Node Server 产物；仅保留既有第三方编辑器分块体积提示 |

final result: passed
